# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
pydantic==2.4.2
pydantic-settings==2.0.3

# HTTP Client
httpx==0.25.1

# LiveKit Integration
livekit==0.10.0
livekit-server-sdk==0.12.0

# Redis for Session State
redis==5.0.1

# Voice Processing (configured via STT_PROVIDER and TTS_PROVIDER env vars)
# Using OpenAI for Whisper STT and TTS (pinned to specific version for security)
openai==1.54.4  # OpenAI Whisper STT and TTS - pinned to latest stable (Jan 2025)
# deepgram-sdk==3.4.0  # Optional: Uncomment if using Deepgram STT
# google-cloud-speech==2.28.0  # Optional: Uncomment if using Google STT
# elevenlabs==1.8.2  # Optional: Uncomment if using ElevenLabs TTS

# Logging and Monitoring
structlog==23.2.0

# Rate Limiting
slowapi==0.1.9  # Rate limiting for FastAPI

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx[http2]==0.25.1
